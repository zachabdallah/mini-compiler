/*
Open your Makefile in Visual Studio Code and use the Command Palette (Ctrl+Shift+P) to run the defined build tasks. For example, you can run the "make test" task to build your project.
*/
/*
# Makefile
FILES = lexer.c parser.c functions.c main.c
CC = g++
CFLAGS = -g -ansi

test: $(FILES)
	$(CC) $(CFLAGS) $(FILES) -o test

lexer.c: lexer.l
	flex lexer.l

parser.c: parser.y lexer.c
	bison parser.y

clean:
	rm -f *.o *~ lexer.c lexer.h parser.c parser.h test
*/
//run this in chatGBT to understand what this means